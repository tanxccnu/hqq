# 咨询需求表单：服务端 CSV/TXT 持久化

## 完成内容
将「华钦麒科技-宣传页面.html」的咨询表单从「纯前端下载 CSV」升级为**服务端持久化**：提交后数据写入服务器本地文件，同时保留无服务器时的前端兜底。

## 改动文件
- **`server.js`（已修改）**
  - `/api/consult` 接口额外接收并写入 `补充说明` 字段；CSV 表头改为 `时间,姓名,单位,电话,需求,补充说明`（6 列），TXT 同步增加「补充说明」行。
  - 默认首页 `/` 改为直接提供 `华钦麒科技-宣传页面.html`（此前是品牌站 index.html）。
- **`华钦麒科技-宣传页面.html`（已修改）**
  - 表单提交改为 `fetch('/api/consult')`——优先服务端保存（CSV/TXT），提交按钮带「提交中…」禁用态；服务端返回失败时显示行内错误红条。
  - 若页面以 `file://` 直接打开（无服务端），自动回退为浏览器端下载 CSV，并提示如何启用后台留存。
  - 新增 `#formError` 行内错误条与 `#successText` 动态成功文案。

## 验证
- 启动服务后 `POST /api/consult` 返回 `{"ok":true}`；`submissions.csv` 写入 6 列（含「约5000卷…」补充说明），`submissions.txt` 同步追加文本记录。
- 已清理测试数据，当前 `submissions.csv` 仅含表头，`submissions.txt` 待首条提交生成。

## 使用方式
```
cd D:\工作文件
node server.js        # 默认端口 3000
```
浏览器打开 `http://localhost:3000`，填写并提交「提交咨询需求」，数据自动追加至 `D:\工作文件\submissions.csv` 与 `submissions.txt`。

## 备注
- 「服务端保存」需运行 `server.js`；直接双击 HTML 打开时会走前端 CSV 下载兜底。
- 服务此前有遗留进程占用 3000 端口，已结束并重启为干净实例。
- 涉密信息仍按《保密法》脱敏，仅留存联系人基础信息与需求类别。
